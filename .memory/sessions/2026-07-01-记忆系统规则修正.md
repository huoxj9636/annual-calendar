# 记忆系统规则修正 - 2026-07-01

## 目录
| 编号 | 主题 | 行号 |
|---|---|---|
| Q1/A1 | 查看整体结构 | L13 |
| Q2/A2 | 新项目vs同项目提示词 | L26 |
| Q3/A3 | 技能包已包含细节无需重复 | L38 |
| Q4/A4 | session文件应每个对话独立 | L49 |

---

## Q1/A1 查看整体结构

**用户问**：这是写到哪里去了？把你整个现有的结构再给我看一下。

**AI答**：读取了 AGENTS.md 全文和 .memory/ 目录下所有文件，整理出完整结构一览：
- AGENTS.md：跨对话记忆系统章节（闭环流程/启动规则/更新流程/推送规则/同步矩阵/规则变更触发条件）→ 项目概述 → 技术栈 → 目录结构 → 核心功能 → 数据流 → 用户红线 → 开发规范
- .memory/：INDEX.md（9条索引）+ PITFALL.md（9条P01-P09）+ sessions/1个详情文件
- cross-session-memory/：SKILL.md + references/agents-config-template.md + scripts/init-memory.sh
- cross-session-memory.skill：已打包的技能文件
- 规则变更触发条件写在 AGENTS.md 第102-106行，位于"记忆系统章节 → 同步矩阵之后 → 项目概述之前"

---

## Q2/A2 新项目vs同项目提示词

**用户问**：我可以理解说以后我要再创建新项目，直接把这个地址发给他就可以了吗？那把你之前给我的那段话重新给我一段。但是我们调整那么多，你重新给我一段。

**AI答**：先回答了新项目用法——发GitHub链接可以，但更简单的是直接说"加载我的跨对话记忆技能，按SKILL.md初始化"，因为技能已保存到"我的技能"。然后给了完整版提示词（含5步：读取规范→初始化.memory/→写入AGENTS.md→记录首个session→commit推送），覆盖了所有调整过的规则。

用户追问：其他对话和当前对话同属一个项目，.memory/是让其他对话重新创建一套还是在已有文件里追加？

**AI答**：同一个项目的所有对话 → 追加到同一套.memory/文件。这恰恰是跨对话记忆系统存在的意义。新项目才需要初始化，同项目其他对话只需要按AGENTS.md启动规则读一遍已有文件。所以提示词应该分两个版本：同项目简洁版（读AGENTS.md然后按启动规则执行）、新项目完整版（初始化全套）。

---

## Q3/A3 技能包已包含细节无需重复

**用户问**：即使是新项目，我也只让他加载按照你说的那个加载技能包按技能包里面的去创建不就行了吗？因为那些不都已经在技能里描述了吗？我理解的对不对？

**AI答**：理解得完全对。技能包里已经写了所有细节——SKILL.md有完整流程，agents-config-template.md有AGENTS.md模板，init-memory.sh有初始化脚本。完整版提示词就是把技能包内容手动抄了一遍，纯属多此一举。最终只需两句话：
- 同一项目其他对话："读取本项目AGENTS.md，按其中跨对话记忆系统的启动规则执行。"
- 新项目："加载我的跨对话记忆技能，按SKILL.md为本项目初始化.memory/目录并写入AGENTS.md。"
剩下的Agent自己读技能包就知道怎么做。之前写得一大堆，反而是对技能包本身的不信任。

---

## Q4/A4 session文件应每个对话独立

**用户问**：我又发现一个问题。你说的那个就是我看一下啊。就是 session memory 里面的 session 你那个周复盘时间轴。对吧？你是你是一个文件。其实那个是基于我们当前对话所创建的一个文件。我最开始的意思就是每一个对话都要有一个独立的文件，只是说它这个文件里面的内容是当前会话所有的内容嘛。那因为我们刚刚提到，就是说 是不是就在已创建的这个文件里面添加内容？那很明显这个详情文件肯定不是的，详情文件是要每个对话独立创建出来一个的。对吧？你这个要搞清楚的。你看你是不是要在规则里面什么都改一下，然后同时让他们把这个事情搞清楚，不要弄混。

**AI答**：用户指出一个根本性区分错误——session详情文件应该是每个对话独立一个文件，绝不能把新对话的内容追加到旧对话的文件里。之前的规则"同一主题延续对话追加到已有文件"是错的。正确规则是：
- INDEX.md / PITFALL.md → 跨对话追加（所有对话共享，汇总层）
- sessions/*.md → 每个对话独立一个文件（绝不追加到别人的文件里，原始记录层）

修正了 AGENTS.md 中的规则、同步更新技能包模板、把错误追加的 Q17-Q20 从旧文件迁移到本新文件、追加PITFALL P10条目。

**用户追问**：你删的 Q17~20是啥呀？是我们当前这个上下文里的内容吗？是的话那就不用删呀。

**AI答**：用户说得对，Q17-Q20是当前上下文真实产生的对话内容，不应该删掉就丢掉。应该从旧文件移到新文件，而不是只删不搬。现在已将完整内容恢复到本文件中（Q1-Q4）。
